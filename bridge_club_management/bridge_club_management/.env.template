# Django settings
DJANGO_SETTINGS_MODULE=bridge_club_management.settings.local
DJANGO_SECRET_KEY=your-local-dev-key-change-me

# Debug settings
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database settings (local SQLite)
DATABASE_URL=sqlite:///db_local.sqlite3

# Email settings (local console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend